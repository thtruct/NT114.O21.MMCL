import { createContext } from 'react';

//
import { ContextType } from './types';

// ----------------------------------------------------------------------

export const ImageSearchContext = createContext({} as ContextType);
